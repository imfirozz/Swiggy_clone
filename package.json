{
  "name": "swiggy-clone-workspace",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix src --include=dev",
    "dev": "npm run dev --prefix src",
    "build": "npm install --prefix src --include=dev && npm run clean --prefix src && npm run build --prefix src",
    "clean": "npm run clean --prefix src"
  }
}
